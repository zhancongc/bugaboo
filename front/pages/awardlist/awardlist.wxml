<!--pages/awardlist/awardlist.wxml-->

<!--view style="margin-top: 20rpx; margin-bottom: 20rpx;" wx:for="{{awards}}" wx:key="{{item.awardrecord_id}}">
  <i-card title="{{item.award_name}}" extra="{{item.award_time}}" thumb="{{item.award_image}}">
  <view slot="content">
  <i-button bind:click="toAward" data-awardrecird_id="{{item.awardrecord_id}}">立即领取</i-button>
  </view>
  </i-card>
</view-->
<i-row>
  <i-col span="10" offset="1">
    <view class="text1">我的奖品</view>
  </i-col>
</i-row>
<view class="section" wx:for="{{awards}}" wx:key="{{item.awardrecord_id}}">
  <i-row>
    <i-col span="4" offset="1">
      <view class="back-image award-image" 
      style='background-image: url({{item.award_image}});'></view>
    </i-col>
    <i-col span="9" offset="1">
      <view class="text2">{{item.award_name}}</view>
      <view class="text3">中奖时间： {{item.award_time}}</view>
    </i-col>
    <i-col span="6" offset="1">
      <view class='back-image list-button {{item.informed ? uninformed ： informed }}' bindtap='toAward' data-awardrecird_id="{{item.awardrecord_id}}">{{item.informed ? '立即领取': '已领取'}}</view>
    </i-col>
    <i-col span="20" offset="2"><view class="divLine"></view></i-col>
  </i-row>
</view>


