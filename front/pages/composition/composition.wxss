/* pages/composition/composition.wxss */
.preview { margin-top: 200rpx; vertical-align: middle; text-align:center; }
.preview image { width: 480rpx; height: 480rpx; }

.angle90 {
  transform:rotate(90deg);
}
.angle180 {
  transform:rotate(180deg);
}
.angle270{
  transform:rotate(270deg);
}